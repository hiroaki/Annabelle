#!/bin/bash
set -e

if [ "$RAILS_ENV" = "development" ]; then
  echo "[docker-entrypoint] Skipping db:prepare in development. Please run manually if needed."
  echo "[docker-entrypoint] mkdir -p log tmp"
  mkdir -p log tmp
else
  echo "Preparing database..."
  ./bin/rails db:prepare
fi

exec "$@"
