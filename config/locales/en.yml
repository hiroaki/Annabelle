# Files in the config/locales directory are used for internationalization and
# are automatically loaded by Rails. If you want to use locales other than
# English, add the necessary files in this directory.
#
# To use the locales, use `I18n.t`:
#
#     I18n.t "hello"
#
# In views, this is aliased to just `t`:
#
#     <%= t("hello") %>
#
# To use a different locale, set it with `I18n.locale`:
#
#     I18n.locale = :es
#
# This would use the information in config/locales/es.yml.
#
# To learn more about the API, please read the Rails Internationalization guide
# at https://guides.rubyonrails.org/i18n.html.
#
# Be aware that YAML interprets the following case-insensitive strings as
# booleans: `true`, `false`, `on`, `off`, `yes`, `no`. Therefore, these strings
# must be quoted to be interpreted as strings. For example:
#
#     en:
#       "yes": yup
#       enabled: "ON"

en:
  views:
    pagination:
      first: "&laquo;"
      last: "&raquo;"
      previous: "&lsaquo;"
      next: "&rsaquo;"
      truncate: "&hellip;"
  helpers:
    page_entries_info:
      one_page:
        display_entries:
          zero: "No %{entry_name} found"
          one: "Displaying <b>1</b> %{entry_name}"
          other: "Displaying <b>all %{count}</b> %{entry_name}"
      more_pages:
        display_entries: "Displaying %{entry_name} <b>%{first}–%{last}</b> of <b>%{total}</b> in total"
  messages:
    errors:
      not_owned: "You cannot delete another user's message."
      generic: "An error occurred: %{error_message}"
    email_confirmation_required: "You cannot post until you confirm your email address."
  activerecord:
    models:
      user: "User"
      message: "Message"
      authorization: "Authorization"
    attributes:
      user:
        email: "Email"
        encrypted_password: "Encrypted password"
        reset_password_token: "Reset password token"
        reset_password_sent_at: "Reset password sent at"
        remember_created_at: "Remember created at"
        created_at: "Created at"
        updated_at: "Updated at"
        admin: "Admin"
        confirmation_token: "Confirmation token"
        confirmed_at: "Confirmed at"
        confirmation_sent_at: "Confirmation sent at"
        unconfirmed_email: "Unconfirmed email"
        username: "Username"
      message:
        content: "Content"
        created_at: "Created at"
        updated_at: "Updated at"
        user_id: "User"
      authorization:
        user_id: "User"
        provider: "Provider"
        uid: "UID"
        created_at: "Created at"
        updated_at: "Updated at"
    errors:
      models:
        user:
          attributes:
            username:
              blank: "can't be blank"
              taken: "has already been taken"
              invalid_format: "can only contain letters, numbers, and underscores."
            email:
              blank: "can't be blank"
              invalid: "is not a valid email address"
        message:
          attributes:
            content:
              blank: "can't be blank"
        authorization:
          attributes:
            provider:
              blank: "can't be blank"
            uid:
              blank: "can't be blank"
              taken: "has already been taken for this provider"
